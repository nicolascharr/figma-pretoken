{
  "name": "pretoken",
  "version": "1.0.0",
  "description": "Pretoken — Figma plugin to detect, cluster and merge hard-coded colors, gradients and text styles before building a design system.",
  "license": "MIT",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
